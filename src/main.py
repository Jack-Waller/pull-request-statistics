"""Entry point for running pull request statistics tooling from the command line."""


def main() -> None:
    """
    Execute the console entry point.

    This placeholder implementation exists so downstream scripts can import
    `main` and hook into the tooling consistently while the application is
    still under construction.
    """
    print("Hello from pull-request-statistics!")


if __name__ == "__main__":
    main()
